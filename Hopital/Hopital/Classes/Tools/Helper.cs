using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;

namespace Hopital.Classes.Tools
{
    public class Helper
    {
        public static bool IsValidNumberSecu(string number)
        {
            bool res = false;

            //res = Regex.IsMatch(, number);

            return res;
        }
    }
}
